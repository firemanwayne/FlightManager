@if (Flight != null)
{
  
}
@code {
    [Parameter] public Flight? Flight { get; set; }

    [Parameter] public EventCallback<FlightTicket> OnSeatSelected { get; set; }

    void SeatClicked(FlightTicket ticket)
    {
        if (OnSeatSelected.HasDelegate)
            OnSeatSelected.InvokeAsync(ticket);

        Console.Write(ticket.Id);
    }
}
